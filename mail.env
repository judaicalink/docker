# Basic settings
DOMAIN=judaicalink.org      # Replace with your actual domain
HOSTNAME=mail           # Hostname of your mail server

# SSL configuration
SSL_TYPE=manual         # Use 'manual' if you're providing your own certs or 'letsencrypt' for auto-SSL
SSL_CERT_PATH=/tmp/docker-mailserver/ssl/fullchain.pem
SSL_KEY_PATH=/tmp/docker-mailserver/ssl/privkey.pem

# Spam & virus settings
ENABLE_SPAMASSASSIN=1   # Enable SpamAssassin for spam filtering
ENABLE_CLAMAV=1         # Enable ClamAV for antivirus
ENABLE_FAIL2BAN=1       # Enable Fail2Ban to protect against brute force
ENABLE_POSTGREY=0       # Disable Postgrey; set to 1 if you want greylisting enabled
